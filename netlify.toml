[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for any path that isn't a real file,
# so client-side routes like /login don't 404 on direct hit or refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
